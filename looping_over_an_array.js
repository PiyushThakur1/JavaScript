let hr=["we", "are", "heroes", "of", "this", "world"];
//for loop
for(let idx=0; idx<hr.length; idx++){
    console.log(hr[idx]);
    
}
//for of
for(let itm of hr){
    console.log(itm.toUpperCase())
}